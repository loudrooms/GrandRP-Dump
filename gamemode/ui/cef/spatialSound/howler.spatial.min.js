/*! howler.js v2.2.4 | Spatial Plugin | (c) 2013-2020, James Simpson of GoldFire Studios | MIT License | howlerjs.com */
(function () {
  "use strict";

  HowlerGlobal.prototype._pos = [0, 0, 0];
  HowlerGlobal.prototype._orientation = [0, 0, -1, 0, 1, 0];
  HowlerGlobal.prototype.stereo = function (e) {
    var n = this;
    if (!n.ctx || !n.ctx.listener) {
      return n;
    }
    for (var t = n._howls.length - 1; t >= 0; t--) {
      n._howls[t].stereo(e);
    }
    return n;
  };
  HowlerGlobal.prototype.pos = function (e, n, t) {
    var r = this;
    if (r.ctx && r.ctx.listener) {
      n = typeof n != "number" ? r._pos[1] : n;
      t = typeof t != "number" ? r._pos[2] : t;
      if (typeof e != "number") {
        return r._pos;
      } else {
        r._pos = [e, n, t];
        if (r.ctx.listener.positionX !== undefined) {
          r.ctx.listener.positionX.setTargetAtTime(r._pos[0], Howler.ctx.currentTime, 0.1);
          r.ctx.listener.positionY.setTargetAtTime(r._pos[1], Howler.ctx.currentTime, 0.1);
          r.ctx.listener.positionZ.setTargetAtTime(r._pos[2], Howler.ctx.currentTime, 0.1);
        } else {
          r.ctx.listener.setPosition(r._pos[0], r._pos[1], r._pos[2]);
        }
        return r;
      }
    } else {
      return r;
    }
  };
  HowlerGlobal.prototype.orientation = function (e, n, t, r, o, i) {
    var a = this;
    if (!a.ctx || !a.ctx.listener) {
      return a;
    }
    var p = a._orientation;
    n = typeof n != "number" ? p[1] : n;
    t = typeof t != "number" ? p[2] : t;
    r = typeof r != "number" ? p[3] : r;
    o = typeof o != "number" ? p[4] : o;
    i = typeof i != "number" ? p[5] : i;
    if (typeof e != "number") {
      return p;
    } else {
      a._orientation = [e, n, t, r, o, i];
      if (a.ctx.listener.forwardX !== undefined) {
        a.ctx.listener.forwardX.setTargetAtTime(e, Howler.ctx.currentTime, 0.1);
        a.ctx.listener.forwardY.setTargetAtTime(n, Howler.ctx.currentTime, 0.1);
        a.ctx.listener.forwardZ.setTargetAtTime(t, Howler.ctx.currentTime, 0.1);
        a.ctx.listener.upX.setTargetAtTime(r, Howler.ctx.currentTime, 0.1);
        a.ctx.listener.upY.setTargetAtTime(o, Howler.ctx.currentTime, 0.1);
        a.ctx.listener.upZ.setTargetAtTime(i, Howler.ctx.currentTime, 0.1);
      } else {
        a.ctx.listener.setOrientation(e, n, t, r, o, i);
      }
      return a;
    }
  };
  Howl.prototype.init = function (e) {
    return function (n) {
      var t = this;
      t._orientation = n.orientation || [1, 0, 0];
      t._stereo = n.stereo || null;
      t._pos = n.pos || null;
      t._pannerAttr = {
        coneInnerAngle: n.coneInnerAngle !== undefined ? n.coneInnerAngle : 360,
        coneOuterAngle: n.coneOuterAngle !== undefined ? n.coneOuterAngle : 360,
        coneOuterGain: n.coneOuterGain !== undefined ? n.coneOuterGain : 0,
        distanceModel: n.distanceModel !== undefined ? n.distanceModel : "inverse",
        maxDistance: n.maxDistance !== undefined ? n.maxDistance : 10000,
        panningModel: n.panningModel !== undefined ? n.panningModel : "HRTF",
        refDistance: n.refDistance !== undefined ? n.refDistance : 1,
        rolloffFactor: n.rolloffFactor !== undefined ? n.rolloffFactor : 1
      };
      t._onstereo = n.onstereo ? [{
        fn: n.onstereo
      }] : [];
      t._onpos = n.onpos ? [{
        fn: n.onpos
      }] : [];
      t._onorientation = n.onorientation ? [{
        fn: n.onorientation
      }] : [];
      return e.call(this, n);
    };
  }(Howl.prototype.init);
  Howl.prototype.stereo = function (n, t) {
    var r = this;
    if (!r._webAudio) {
      return r;
    }
    if (r._state !== "loaded") {
      r._queue.push({
        event: "stereo",
        action: function () {
          r.stereo(n, t);
        }
      });
      return r;
    }
    var o = Howler.ctx.createStereoPanner === undefined ? "spatial" : "stereo";
    if (t === undefined) {
      if (typeof n != "number") {
        return r._stereo;
      }
      r._stereo = n;
      r._pos = [n, 0, 0];
    }
    for (var i = r._getSoundIds(t), a = 0; a < i.length; a++) {
      var p = r._soundById(i[a]);
      if (p) {
        if (typeof n != "number") {
          return p._stereo;
        }
        p._stereo = n;
        p._pos = [n, 0, 0];
        if (p._node) {
          p._pannerAttr.panningModel = "equalpower";
          if (!p._panner || !p._panner.pan) {
            e(p, o);
          }
          if (o === "spatial") {
            if (p._panner.positionX !== undefined) {
              p._panner.positionX.setValueAtTime(n, Howler.ctx.currentTime);
              p._panner.positionY.setValueAtTime(0, Howler.ctx.currentTime);
              p._panner.positionZ.setValueAtTime(0, Howler.ctx.currentTime);
            } else {
              p._panner.setPosition(n, 0, 0);
            }
          } else {
            p._panner.pan.setValueAtTime(n, Howler.ctx.currentTime);
          }
        }
        r._emit("stereo", p._id);
      }
    }
    return r;
  };
  Howl.prototype.pos = function (n, t, r, o) {
    var i = this;
    if (!i._webAudio) {
      return i;
    }
    if (i._state !== "loaded") {
      i._queue.push({
        event: "pos",
        action: function () {
          i.pos(n, t, r, o);
        }
      });
      return i;
    }
    t = typeof t != "number" ? 0 : t;
    r = typeof r != "number" ? -0.5 : r;
    if (o === undefined) {
      if (typeof n != "number") {
        return i._pos;
      }
      i._pos = [n, t, r];
    }
    for (var a = i._getSoundIds(o), p = 0; p < a.length; p++) {
      var s = i._soundById(a[p]);
      if (s) {
        if (typeof n != "number") {
          return s._pos;
        }
        s._pos = [n, t, r];
        if (s._node) {
          if (!s._panner || !!s._panner.pan) {
            e(s, "spatial");
          }
          if (s._panner.positionX !== undefined) {
            s._panner.positionX.setValueAtTime(n, Howler.ctx.currentTime);
            s._panner.positionY.setValueAtTime(t, Howler.ctx.currentTime);
            s._panner.positionZ.setValueAtTime(r, Howler.ctx.currentTime);
          } else {
            s._panner.setPosition(n, t, r);
          }
        }
        i._emit("pos", s._id);
      }
    }
    return i;
  };
  Howl.prototype.orientation = function (n, t, r, o) {
    var i = this;
    if (!i._webAudio) {
      return i;
    }
    if (i._state !== "loaded") {
      i._queue.push({
        event: "orientation",
        action: function () {
          i.orientation(n, t, r, o);
        }
      });
      return i;
    }
    t = typeof t != "number" ? i._orientation[1] : t;
    r = typeof r != "number" ? i._orientation[2] : r;
    if (o === undefined) {
      if (typeof n != "number") {
        return i._orientation;
      }
      i._orientation = [n, t, r];
    }
    for (var a = i._getSoundIds(o), p = 0; p < a.length; p++) {
      var s = i._soundById(a[p]);
      if (s) {
        if (typeof n != "number") {
          return s._orientation;
        }
        s._orientation = [n, t, r];
        if (s._node) {
          if (!s._panner) {
            s._pos ||= i._pos || [0, 0, -0.5];
            e(s, "spatial");
          }
          if (s._panner.orientationX !== undefined) {
            s._panner.orientationX.setValueAtTime(n, Howler.ctx.currentTime);
            s._panner.orientationY.setValueAtTime(t, Howler.ctx.currentTime);
            s._panner.orientationZ.setValueAtTime(r, Howler.ctx.currentTime);
          } else {
            s._panner.setOrientation(n, t, r);
          }
        }
        i._emit("orientation", s._id);
      }
    }
    return i;
  };
  Howl.prototype.pannerAttr = function () {
    var n;
    var t;
    var r;
    var o = this;
    var i = arguments;
    if (!o._webAudio) {
      return o;
    }
    if (i.length === 0) {
      return o._pannerAttr;
    }
    if (i.length === 1) {
      if (typeof i[0] != "object") {
        r = o._soundById(parseInt(i[0], 10));
        if (r) {
          return r._pannerAttr;
        } else {
          return o._pannerAttr;
        }
      }
      n = i[0];
      if (t === undefined) {
        n.pannerAttr ||= {
          coneInnerAngle: n.coneInnerAngle,
          coneOuterAngle: n.coneOuterAngle,
          coneOuterGain: n.coneOuterGain,
          distanceModel: n.distanceModel,
          maxDistance: n.maxDistance,
          refDistance: n.refDistance,
          rolloffFactor: n.rolloffFactor,
          panningModel: n.panningModel
        };
        o._pannerAttr = {
          coneInnerAngle: n.pannerAttr.coneInnerAngle !== undefined ? n.pannerAttr.coneInnerAngle : o._coneInnerAngle,
          coneOuterAngle: n.pannerAttr.coneOuterAngle !== undefined ? n.pannerAttr.coneOuterAngle : o._coneOuterAngle,
          coneOuterGain: n.pannerAttr.coneOuterGain !== undefined ? n.pannerAttr.coneOuterGain : o._coneOuterGain,
          distanceModel: n.pannerAttr.distanceModel !== undefined ? n.pannerAttr.distanceModel : o._distanceModel,
          maxDistance: n.pannerAttr.maxDistance !== undefined ? n.pannerAttr.maxDistance : o._maxDistance,
          refDistance: n.pannerAttr.refDistance !== undefined ? n.pannerAttr.refDistance : o._refDistance,
          rolloffFactor: n.pannerAttr.rolloffFactor !== undefined ? n.pannerAttr.rolloffFactor : o._rolloffFactor,
          panningModel: n.pannerAttr.panningModel !== undefined ? n.pannerAttr.panningModel : o._panningModel
        };
      }
    } else if (i.length === 2) {
      n = i[0];
      t = parseInt(i[1], 10);
    }
    for (var a = o._getSoundIds(t), p = 0; p < a.length; p++) {
      if (r = o._soundById(a[p])) {
        var s = r._pannerAttr;
        s = {
          coneInnerAngle: n.coneInnerAngle !== undefined ? n.coneInnerAngle : s.coneInnerAngle,
          coneOuterAngle: n.coneOuterAngle !== undefined ? n.coneOuterAngle : s.coneOuterAngle,
          coneOuterGain: n.coneOuterGain !== undefined ? n.coneOuterGain : s.coneOuterGain,
          distanceModel: n.distanceModel !== undefined ? n.distanceModel : s.distanceModel,
          maxDistance: n.maxDistance !== undefined ? n.maxDistance : s.maxDistance,
          refDistance: n.refDistance !== undefined ? n.refDistance : s.refDistance,
          rolloffFactor: n.rolloffFactor !== undefined ? n.rolloffFactor : s.rolloffFactor,
          panningModel: n.panningModel !== undefined ? n.panningModel : s.panningModel
        };
        var c = r._panner;
        if (!c) {
          r._pos ||= o._pos || [0, 0, -0.5];
          e(r, "spatial");
          c = r._panner;
        }
        c.coneInnerAngle = s.coneInnerAngle;
        c.coneOuterAngle = s.coneOuterAngle;
        c.coneOuterGain = s.coneOuterGain;
        c.distanceModel = s.distanceModel;
        c.maxDistance = s.maxDistance;
        c.refDistance = s.refDistance;
        c.rolloffFactor = s.rolloffFactor;
        c.panningModel = s.panningModel;
      }
    }
    return o;
  };
  Sound.prototype.init = function (e) {
    return function () {
      var n = this;
      var t = n._parent;
      n._orientation = t._orientation;
      n._stereo = t._stereo;
      n._pos = t._pos;
      n._pannerAttr = t._pannerAttr;
      e.call(this);
      if (n._stereo) {
        t.stereo(n._stereo);
      } else if (n._pos) {
        t.pos(n._pos[0], n._pos[1], n._pos[2], n._id);
      }
    };
  }(Sound.prototype.init);
  Sound.prototype.reset = function (e) {
    return function () {
      var n = this;
      var t = n._parent;
      n._orientation = t._orientation;
      n._stereo = t._stereo;
      n._pos = t._pos;
      n._pannerAttr = t._pannerAttr;
      if (n._stereo) {
        t.stereo(n._stereo);
      } else if (n._pos) {
        t.pos(n._pos[0], n._pos[1], n._pos[2], n._id);
      } else if (n._panner) {
        n._panner.disconnect(0);
        n._panner = undefined;
        t._refreshBuffer(n);
      }
      return e.call(this);
    };
  }(Sound.prototype.reset);
  function e(e, n) {
    n = n || "spatial";
    if (n === "spatial") {
      e._panner = Howler.ctx.createPanner();
      e._panner.coneInnerAngle = e._pannerAttr.coneInnerAngle;
      e._panner.coneOuterAngle = e._pannerAttr.coneOuterAngle;
      e._panner.coneOuterGain = e._pannerAttr.coneOuterGain;
      e._panner.distanceModel = e._pannerAttr.distanceModel;
      e._panner.maxDistance = e._pannerAttr.maxDistance;
      e._panner.refDistance = e._pannerAttr.refDistance;
      e._panner.rolloffFactor = e._pannerAttr.rolloffFactor;
      e._panner.panningModel = e._pannerAttr.panningModel;
      if (e._panner.positionX !== undefined) {
        e._panner.positionX.setValueAtTime(e._pos[0], Howler.ctx.currentTime);
        e._panner.positionY.setValueAtTime(e._pos[1], Howler.ctx.currentTime);
        e._panner.positionZ.setValueAtTime(e._pos[2], Howler.ctx.currentTime);
      } else {
        e._panner.setPosition(e._pos[0], e._pos[1], e._pos[2]);
      }
      if (e._panner.orientationX !== undefined) {
        e._panner.orientationX.setValueAtTime(e._orientation[0], Howler.ctx.currentTime);
        e._panner.orientationY.setValueAtTime(e._orientation[1], Howler.ctx.currentTime);
        e._panner.orientationZ.setValueAtTime(e._orientation[2], Howler.ctx.currentTime);
      } else {
        e._panner.setOrientation(e._orientation[0], e._orientation[1], e._orientation[2]);
      }
    } else {
      e._panner = Howler.ctx.createStereoPanner();
      e._panner.pan.setValueAtTime(e._stereo, Howler.ctx.currentTime);
    }
    e._panner.connect(e._node);
    if (!e._paused) {
      e._parent.pause(e._id, true).play(e._id, true);
    }
  }
})();